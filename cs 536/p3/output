int g;
double g;
void g;
void f() {
}
int f(int x) {
}
double f(double y) {
}
void f(void x, int y) {
}
int g;
double g;
void g;
int foo() {
 int x;
 int y;
 double z;
 _ = (_ = 10);
 if( (x = 22)) {
 }
 ++anotherId_;
 --_1;
 thisIsAnId++;
 hello2--;
 arm_1--;
 scanf("%d", &x);
 scanf("%f", &x);
 printf("%d", 10);
 printf("%f", f);
 printf("with \\ escaped");
 if( 0) {
 }
 if( (a > b)) {
  scanf("%f", &x);
 }
 if( (a != b)) {
  printf("%d", x);
 }
 if( 1) {
 }
 else {
 }
 if( (x == y)) {
  x--;
 }
 else {
  x = (y + 1);
  z = (-w);
 }
 while( true) {
 }
 while( x) {
  ++x;
 }
 return 22;
 return "hello";
 return (x + y);
 return ;
 x();
 x(1, 2, 3);
 x = (y + 10);
 if( (x - 22)) {
 }
 while( (10 * dd)) {
 }
 if( (a / b)) {
 }
 else {
 }
 x = (!y);
 y = (a && b);
 printf("%d", (1 || 0));
 if( (0 == 22)) {
 }
 if( (0 != 22)) {
 }
 if( (0 < 22)) {
 }
 x = (y > z);
 x = (y <= z);
 x = (y >= z);
 x = (-1);
 if( ((-1) * (-22))) {
 }
 x = y++;
 if( (y++ - z++)) {
 }
 x = y--;
 if( (y-- - z--)) {
 }
 x = 15;
 x = 22.5;
 x = 0.33;
 x = 34.0;
 x = "hello";
 x = 5;
 x = foo();
 x = bar(23, 44);
}
