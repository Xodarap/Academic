module hazarddetector(XMemRead, XRt, DRs, DRt, Stall);
   input XMemRead;
   input [2:0] XRt, DRs, DRt;
   output Stall;

   assign Stall = 1'b0;
//XMemRead & (XRt == DRs | XRt == DRt);
   
endmodule // hazarddetector
